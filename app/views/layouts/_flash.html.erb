<% if flash.any? %>
  <div class="bg-yellow-100 border-yellow-500 text-yellow-700 p-3" role="alert">
    <% flash.each do |type, message| %>
      <div><%= message %></div>
    <% end %>
  </div>
<% end %>
